from django.shortcuts import render
from mini_crm.models import Order

#class BasketListView(TemplateResponseMixin,View):
   # template_name = 'clothes/client-site/bag.html'

    #def get (self, request):
     #   goods = Order.objects.filter(is_top=True)
     #   return self.render_to_response({
      #      'categories': categories, 'goods': goods, 'demand_form': demand_form})

